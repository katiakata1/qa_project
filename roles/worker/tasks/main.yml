---
- name: Add worker to manager node
  docker_swarm:
    state: join
    advertise_addr: worker
    join_token: "{{ hostvars['34.83.163.142']['swarm_info']['swarm_facts']['JoinTokens']['Worker']  }}"
    remote_addrs: ['swarm-manager:2377']
